from django.apps import AppConfig

class VolunteerSlotsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'volunteer_slots'
    verbose_name = "LEGACY volunteer slot migrations"
