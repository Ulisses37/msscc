from django.apps import AppConfig

class VolunteerSignupsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'volunteer_signup'
    verbose_name = "LEGACY volunteer signup migrations"
